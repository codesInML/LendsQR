apps:
  - script: build/index.js             # Application's startup file name
    instances: 2                    # Number of processes that must run in parallel, you can change this if you want
    exec_mode: cluster              # Execution mode
